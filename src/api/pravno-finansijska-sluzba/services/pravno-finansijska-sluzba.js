'use strict';

/**
 * pravno-finansijska-sluzba service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pravno-finansijska-sluzba.pravno-finansijska-sluzba');
